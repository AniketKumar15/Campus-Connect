code 1
import React from "react";
import { motion } from "framer-motion";
import {
  FiHome,
  FiBookOpen,
  FiUpload,
  FiEdit,
  FiUser,
  FiActivity,
} from "react-icons/fi";
import { HiLightningBolt } from "react-icons/hi";
import logo from "../assets/Logo.png";

const quickActions = [
  { title: "Submit Assignment", icon: <FiUpload /> },
  { title: "Upload Resource", icon: <FiBookOpen /> },
  { title: "Write Blog", icon: <FiEdit /> },
  { title: "Edit Profile", icon: <FiUser /> },
];

const assignments = [
  { subject: "DBMS", title: "ER Diagram", due: "Jan 25", status: "Pending" },
  { subject: "OS", title: "Process Scheduling", due: "Jan 28", status: "Submitted" },
];

const resources = [
  { title: "React Hooks Notes", author: "Prof. Sharma" },
  { title: "Operating System PPT", author: "Prof. Khan" },
];

export default function StudentDashboard() {
  return (
    <div className="min-h-screen flex bg-[#0B0F1A] text-slate-100 overflow-hidden">
      {/* 🔥 SIDEBAR – COMMAND CENTER */}
      <aside className="hidden md:flex w-72 flex-col bg-gradient-to-b from-[#0E1324] to-[#05070F] border-r border-white/10">
        {/* Logo */}
        <div className="flex items-center gap-3 px-6 py-6">
          <img src={logo} alt="Campus Connect" className="w-10 h-10" />
          <div>
            <h1 className="font-bold tracking-wide">Campus Connect</h1>
            <p className="text-xs text-slate-400">Mass Mode</p>
          </div>
        </div>

        {/* Nav */}
        <nav className="flex-1 px-4 space-y-2 text-sm">
          {[
            { label: "Dashboard", icon: <FiHome /> },
            { label: "Assignments", icon: <FiActivity /> },
            { label: "Resources", icon: <FiBookOpen /> },
            { label: "Blogs", icon: <FiEdit /> },
            { label: "Profile", icon: <FiUser /> },
          ].map((item, i) => (
            <motion.div
              whileHover={{ scale: 1.05 }}
              key={i}
              className={`flex items-center gap-3 px-4 py-3 rounded-xl cursor-pointer
                ${
                  i === 0
                    ? "bg-gradient-to-r from-indigo-500/20 to-cyan-500/20 text-white"
                    : "text-slate-400 hover:text-white hover:bg-white/5"
                }`}
            >
              <span className="text-lg">{item.icon}</span>
              {item.label}
            </motion.div>
          ))}
        </nav>
      </aside>

      {/* 🔥 MAIN */}
      <div className="flex-1 flex flex-col">
        {/* HERO HEADER */}
        <header className="relative overflow-hidden">
          <div className="absolute inset-0 bg-gradient-to-r from-indigo-600/20 to-cyan-500/20 blur-3xl" />
          <div className="relative px-8 py-8 flex justify-between items-center">
            <div>
              <motion.h2
                initial={{ y: 20, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                className="text-3xl font-extrabold tracking-wide"
              >
                Welcome Back, Rahul
              </motion.h2>
              <p className="text-slate-400 mt-1">
                Ready to dominate today? ⚡
              </p>
            </div>

            <div className="flex items-center gap-4">
              <HiLightningBolt className="text-yellow-400 text-2xl animate-pulse" />
              <div className="w-10 h-10 rounded-full bg-gradient-to-r from-indigo-500 to-cyan-500" />
            </div>
          </div>
        </header>

        {/* CONTENT */}
        <main className="p-8 space-y-10">
          {/* ⚡ QUICK ACTIONS */}
          <section>
            <h3 className="mb-4 font-semibold tracking-wide">Quick Actions</h3>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
              {quickActions.map((a, i) => (
                <motion.div
                  key={i}
                  whileHover={{ y: -6, scale: 1.05 }}
                  className="relative overflow-hidden rounded-2xl bg-white/5 backdrop-blur-xl border border-white/10 p-6 cursor-pointer"
                >
                  <div className="absolute inset-0 bg-gradient-to-br from-indigo-500/20 to-cyan-500/20 opacity-0 hover:opacity-100 transition" />
                  <div className="relative z-10">
                    <div className="text-3xl mb-3 text-cyan-400">
                      {a.icon}
                    </div>
                    <p className="font-medium">{a.title}</p>
                  </div>
                </motion.div>
              ))}
            </div>
          </section>

          {/* 🎯 ASSIGNMENTS + RESOURCES */}
          <section className="grid grid-cols-1 lg:grid-cols-2 gap-8">
            {/* Assignments */}
            <div className="rounded-2xl bg-white/5 border border-white/10 backdrop-blur-xl">
              <div className="px-6 py-4 font-semibold border-b border-white/10">
                Upcoming Assignments
              </div>
              <ul>
                {assignments.map((a, i) => (
                  <li
                    key={i}
                    className="px-6 py-4 flex justify-between hover:bg-white/5 transition"
                  >
                    <div>
                      <p className="font-medium">{a.title}</p>
                      <p className="text-xs text-slate-400">{a.subject}</p>
                    </div>
                    <span
                      className={`text-xs px-3 py-1 rounded-full self-center ${
                        a.status === "Submitted"
                          ? "bg-emerald-500/20 text-emerald-400"
                          : "bg-amber-500/20 text-amber-400"
                      }`}
                    >
                      {a.status}
                    </span>
                  </li>
                ))}
              </ul>
            </div>

            {/* Resources */}
            <div className="rounded-2xl bg-white/5 border border-white/10 backdrop-blur-xl">
              <div className="px-6 py-4 font-semibold border-b border-white/10">
                New Resources
              </div>
              <ul>
                {resources.map((r, i) => (
                  <li
                    key={i}
                    className="px-6 py-4 hover:bg-white/5 transition"
                  >
                    <p className="font-medium">{r.title}</p>
                    <p className="text-xs text-slate-400">
                      by {r.author}
                    </p>
                  </li>
                ))}
              </ul>
            </div>
          </section>
        </main>
      </div>
    </div>
  );
} 
code 2
import React from "react";
import { motion } from "framer-motion";
import {
  FiHome,
  FiBookOpen,
  FiUpload,
  FiEdit,
  FiUser,
  FiZap,
} from "react-icons/fi";
import logo from "../assets/Logo.png";

const actions = [
  { title: "Submit Assignment", icon: <FiUpload /> },
  { title: "Upload Resource", icon: <FiBookOpen /> },
  { title: "Write Blog", icon: <FiEdit /> },
  { title: "Edit Profile", icon: <FiUser /> },
];

export default function StudentDashboard() {
  return (
    <div className="relative min-h-screen bg-[#05070F] text-white overflow-hidden">

      {/* 🔥 BACKGROUND ENERGY */}
      <div className="absolute inset-0">
        <div className="absolute -top-40 -left-40 w-[500px] h-[500px] bg-indigo-600/30 rounded-full blur-[120px]" />
        <div className="absolute top-1/2 right-0 w-[400px] h-[400px] bg-cyan-500/20 rounded-full blur-[120px]" />
      </div>

      <div className="relative z-10 flex">

        {/* 🧭 FLOATING SIDEBAR */}
        <aside className="hidden md:flex flex-col w-24 items-center py-8 gap-10">
          <motion.img
            src={logo}
            alt="Campus Connect"
            className="w-12 h-12"
            initial={{ rotate: -180, opacity: 0 }}
            animate={{ rotate: 0, opacity: 1 }}
          />

          {[FiHome, FiZap, FiBookOpen, FiEdit, FiUser].map(
            (Icon, i) => (
              <motion.div
                key={i}
                whileHover={{ scale: 1.3, rotate: 5 }}
                className={`w-14 h-14 flex items-center justify-center rounded-2xl
                ${
                  i === 0
                    ? "bg-gradient-to-br from-indigo-500 to-cyan-500"
                    : "bg-white/5 hover:bg-white/10"
                } backdrop-blur-xl cursor-pointer`}
              >
                <Icon className="text-xl" />
              </motion.div>
            )
          )}
        </aside>

        {/* 🏛 MAIN AREA */}
        <main className="flex-1 px-10 py-10 space-y-14">

          {/* 👑 HERO TITLE */}
          <section>
            <motion.h1
              initial={{ opacity: 0, y: 40 }}
              animate={{ opacity: 1, y: 0 }}
              className="text-5xl font-black tracking-tight leading-tight"
            >
              Campus Connect
              <span className="block text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 to-cyan-400">
                Student Command Center
              </span>
            </motion.h1>

            <p className="mt-4 text-slate-400 max-w-xl">
              This isn’t a dashboard.  
              This is where **deadlines fear you** ⚡
            </p>
          </section>

          {/* ⚡ ACTION CARDS */}
          <section className="grid grid-cols-2 md:grid-cols-4 gap-8">
            {actions.map((a, i) => (
              <motion.div
                key={i}
                whileHover={{ y: -12, scale: 1.08 }}
                whileTap={{ scale: 0.95 }}
                className="relative rounded-3xl p-8 bg-white/5 border border-white/10 backdrop-blur-xl cursor-pointer overflow-hidden"
              >
                {/* Glow */}
                <div className="absolute inset-0 bg-gradient-to-br from-indigo-500/30 to-cyan-500/30 opacity-0 hover:opacity-100 transition" />

                <div className="relative z-10">
                  <div className="text-4xl mb-4 text-cyan-400">
                    {a.icon}
                  </div>
                  <h3 className="font-semibold tracking-wide">
                    {a.title}
                  </h3>
                </div>
              </motion.div>
            ))}
          </section>

          {/* 🧠 STATUS PANEL */}
          <section className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              { label: "Assignments Pending", value: "02" },
              { label: "Resources Uploaded", value: "18" },
              { label: "Blogs Written", value: "05" },
            ].map((stat, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: i * 0.15 }}
                className="rounded-3xl p-6 bg-gradient-to-br from-white/5 to-white/0 border border-white/10 backdrop-blur-xl"
              >
                <p className="text-sm text-slate-400">{stat.label}</p>
                <p className="text-4xl font-extrabold mt-2">
                  {stat.value}
                </p>
              </motion.div>
            ))}
          </section>

        </main>
      </div>
    </div>
  );
}

now my idea is combine these to code and make a new one, why becuse some of the things i like in the code 1 like side bar(becuse i can add so many option and also easy to navigate due to icon + text combo), but color and card text every other things i like from code 2. 




{ label: "Assignments", icon: FiClock, path: "/dashboard/assignments" },
